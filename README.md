# Fly-in
**Fly-in** is a drone routing simulator that finds efficient paths for multiple drones across a network of connected zones. The system optimizes movement scheduling, avoids conflicts, respects capacity constraints, and aims to minimize the total number of simulation turns required to reach the destination.
